apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "flask-counter.fullname" . }}-config
  labels:
    {{ include "flask-counter.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.configMap.data }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}